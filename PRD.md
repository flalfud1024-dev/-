# 도서 리뷰 웹 크롤링 도구 PRD

## 1. 개요

예스24에 등록된 도서 리뷰(회원리뷰 + 한줄평)를 대량으로 수집해 Excel/CSV로
저장하는 로컬 실행형 크롤러. 1차 대상 도서는 한강 『채식주의자』이며, 향후
다른 도서로 확장할 수 있도록 설계한다.

## 2. 배경 및 목적

- 독자 반응/감성 분석 (긍정·부정, 키워드, 시계열 추이 등)
- 학술/연구용 데이터셋 구축
- 비프로그래머도 윈도우 PC에서 실행할 수 있어야 함

## 3. 사용자 (Persona)

- 프로그래밍 경험이 거의 없는 연구자/기획자
- Windows PC 환경
- Python/터미널 기본 사용은 가능 (간단한 설치 가이드 제공 필요)

## 4. 범위

### 4.1 In-Scope (1차 MVP)

- 사이트: **예스24** 만 지원
- 도서: 한강 『채식주의자』 (ISBN/URL 기준)
- 리뷰 종류:
  - **회원리뷰** (긴 글)
  - **한줄평**
- 수집 범위: 등록된 **전체 리뷰**
- 로그인 필요 콘텐츠 접근: **사용자 본인 계정 로그인** 지원
- 출력: **Excel(.xlsx) / CSV(.csv)** 두 가지 동시 저장

### 4.2 Out-of-Scope (1차 제외)

- 교보문고, 알라딘, 네이버 책 등 타 사이트
- 웹 GUI / 서버 자동 스케줄링
- 실시간 모니터링·알림
- AI 분석 기능 (분석은 별도 단계)

### 4.3 Future (확장 고려)

- 도서 URL/ISBN만 바꿔 다른 책 수집
- 책 리스트 파일(CSV)을 입력받아 일괄 수집
- 수집 결과 증분 업데이트(이미 받은 리뷰는 스킵)

## 5. 수집 데이터 항목

| 항목                | 설명                                    | 비고               |
| ------------------- | --------------------------------------- | ------------------ |
| review_id           | 리뷰 고유 ID(또는 URL 해시)             | 중복 방지 키       |
| book_title          | 책 제목                                 |                    |
| book_isbn           | ISBN                                    |                    |
| review_type         | "회원리뷰" / "한줄평"                   |                    |
| review_text         | 리뷰 본문 전체 텍스트                   | 줄바꿈 보존        |
| rating              | 별점/평점 (있을 경우)                   | 없으면 빈 값       |
| like_count          | 좋아요 수                               |                    |
| view_count          | 조회수 (제공되는 경우)                  |                    |
| author_nickname     | 작성자 닉네임 (원본)                    | 개인정보           |
| author_nickname_hash| 작성자 닉네임 해시(SHA-256 등)          | 익명화 버전        |
| created_at          | 작성일                                  | YYYY-MM-DD         |
| collected_at        | 수집 시각                               | ISO 8601           |
| source_url          | 원본 리뷰 페이지 URL                    |                    |

> **개인정보 처리:** 닉네임은 원본과 익명화 해시를 동시에 저장한다. 학술/연구
> 공개 시에는 해시 컬럼만 사용하고 원본 컬럼은 제거한 별도 파일을 내보낸다.

## 6. 기능 요구사항

### 6.1 실행 방식

- Windows 환경에서 **터미널/CMD로 실행**
- 사용 예:
  ```
  python scraper.py --book "채식주의자"
  python scraper.py --url "https://www.yes24.com/Product/Goods/XXXXXXXX"
  ```
- 설치 가이드(Python 설치, 의존 패키지 설치, 첫 실행)를 README에 단계별로 제공

### 6.2 로그인

- 본인 예스24 계정으로 로그인하여 수집
- 계정 정보는 `.env` 파일 또는 첫 실행 시 입력 → 로컬에만 저장
- 가능하면 **세션 쿠키 저장**으로 매번 로그인 반복 회피
- 캡차 발생 시 사용자에게 알리고 일시 정지

### 6.3 수집 동작

- 회원리뷰 / 한줄평 각각의 목록 페이지를 순회하며 끝까지 수집
- 각 리뷰의 상세(긴 본문이 접혀 있는 경우 펼친 후 수집)
- **요청 간 1~3초 랜덤 지연** (기본값, 서버 부담 최소화)
- 페이지 단위 진행률 표시 (예: `회원리뷰 12/87 페이지`)
- 중간 실패 시 마지막 성공 지점부터 **재시작 가능** (체크포인트 저장)

### 6.4 출력

- 결과 파일은 `output/` 폴더에 저장
- 파일명 규칙:
  ```
  output/{ISBN}_{책제목}_{YYYYMMDD_HHMM}.xlsx
  output/{ISBN}_{책제목}_{YYYYMMDD_HHMM}.csv
  ```
- 동일 컬럼 구조의 Excel과 CSV 두 파일 모두 생성
- CSV 인코딩: **UTF-8 with BOM** (Windows Excel 한글 깨짐 방지)
- 학술 공개용 익명화 파일은 `--anonymized` 옵션으로 별도 생성

### 6.5 로깅

- 진행 상황과 오류를 `logs/run_{timestamp}.log`에 기록
- 차단·캡차·네트워크 오류 등 사유를 명확히 남김

## 7. 비기능 요구사항

- **속도/부하:** 기본 요청 간격 1~3초, 동시 요청 1개 (단일 스레드)
- **에러 처리:** 네트워크 오류 시 지수 백오프(2s → 4s → 8s) 최대 3회 재시도
- **안정성:** 한 책 전체 수집 중 한 페이지 실패해도 전체가 중단되지 않음
- **재현성:** 동일 입력에 대해 동일 컬럼/형식의 결과
- **확장성:** 책 식별자(URL/ISBN)만 바꾸면 다른 책에도 동작
- **준법:**
  - robots.txt 및 사이트 이용약관을 확인하고 위배 시 사용자에게 경고
  - 수집 데이터는 개인 사용/연구 목적 한정, 재배포 금지 안내를 README에 명시

## 8. 기술 스택 (제안)

- 언어: **Python 3.11+**
- HTTP/렌더링:
  - 정적 HTML이면 `requests` + `beautifulsoup4`
  - 자바스크립트 렌더링/로그인 필요 시 `playwright` (헤드리스 크롬)
- 출력: `pandas` + `openpyxl`
- 환경 변수: `python-dotenv`
- 로깅: 표준 `logging`

## 9. 산출물

1. `scraper.py` (메인 실행 스크립트)
2. `requirements.txt`
3. `.env.example` (계정 정보 템플릿)
4. `README.md` (Windows 설치/실행 가이드, 캡처 포함)
5. `output/`, `logs/` 폴더 자동 생성
6. 샘플 결과 파일 (테스트 50개 수집본)

## 10. 마일스톤

| 단계 | 내용                                           | 산출물                   |
| ---- | ---------------------------------------------- | ------------------------ |
| M1   | URL 구조 분석 + 50개 시범 수집 (비로그인)      | 동작 PoC, 샘플 CSV       |
| M2   | 로그인/세션 처리, 한줄평 수집 추가             | 두 종류 모두 수집 가능   |
| M3   | 전체 리뷰 수집 + 체크포인트/재시작             | 안정 버전                |
| M4   | Excel/CSV·익명화 출력, 로그, README            | 1.0 릴리스               |
| M5   | 다른 책 확장(URL 인자화), 책 리스트 일괄 수집  | 1.1                      |

## 11. 성공 기준

- 『채식주의자』 회원리뷰·한줄평을 **누락 없이** 한 번에 전체 수집
- 결과 파일이 Excel에서 한글 깨짐 없이 열림
- 수집 중 IP 차단·계정 제재 발생 0건
- 비프로그래머가 README만 보고 설치~첫 수집까지 30분 내에 완료

## 12. 리스크 및 대응

| 리스크                          | 대응                                                          |
| ------------------------------- | ------------------------------------------------------------- |
| 사이트 HTML 구조 변경           | 셀렉터를 한 곳에 모아두고 변경 시 그 부분만 수정              |
| 로그인 차단/캡차                | 즉시 중단, 사용자에게 알림. 쿠키 수동 입력 모드 폴백          |
| 과도한 요청으로 IP 차단         | 기본 지연 1~3초 유지, 재시도 횟수 제한                        |
| 개인정보(닉네임) 유출           | 공개용은 항상 익명화 파일만 사용. 원본 파일 별도 보관 안내    |
| 약관 변경으로 수집이 부적합     | README에 사용자 책임 고지, 약관 검토 절차 문서화              |

## 13. 결정이 필요한 미해결 항목

- 회원리뷰 본문에 포함된 **이미지/별점 아이콘**의 처리 방식 (텍스트만 vs URL 별도 컬럼)
- 같은 작성자가 같은 책에 여러 리뷰를 단 경우 모두 보존(현재 안)인지 최신만 보존인지
- 리뷰 페이지가 매우 많을 경우(예: 5,000건+) 한 번에 끝까지 받을지, 일자 기준으로 분할 수집할지
