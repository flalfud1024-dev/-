[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# /api/transcribe → /.netlify/functions/transcribe
# /api/translate  → /.netlify/functions/translate
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
